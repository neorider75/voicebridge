# VoiceBridge Worker — Python deps (CUDA 12.1, PyTorch 2.4)

# RunPod SDK
runpod>=1.7

# PyTorch + torchaudio CUDA 12.1 — installés en RUN dédié dans Dockerfile
# (pip install ... --index-url https://download.pytorch.org/whl/cu121).
# Pas de pin ici : le suffixe +cu121 n'est plus géré par pip moderne via
# --extra-index-url (pip cherche sur PyPI et ne trouve pas la wheel CUDA).

# Transformers ecosystem
# /!\ Le Dockerfile désinstalle fbgemm_gpu + torchao après ce pip install
# (transitive deps de transformers/accelerate qui exigent torch 2.5+, on est
# sur 2.4). Symptôme sans le uninstall : "infer_schema unsupported type
# torch.Tensor" au load NLLB. Sans elles, transformers utilise ses fallbacks.
transformers>=4.46
accelerate>=0.30
huggingface-hub>=0.26
sentencepiece>=0.2     # tokenizers NLLB
sacremoses>=0.1        # tokenizers OPUS-MT

# CTranslate2 pour optimisation Whisper / OPUS-MT
ctranslate2>=4.4
faster-whisper>=1.0

# Quantization
# /!\ Pin strict 0.43.3 : bitsandbytes 0.44+ utilise une API d'enregistrement
# de kernel CUDA introduite pour torch 2.5+ et incompatible avec torch 2.4.
# Symptôme avec >=0.44 : "infer_schema(func): Parameter input has unsupported
# type torch.Tensor" au load NLLB (qui utilise bnb pour quantization INT8).
# 0.43.3 = dernière 0.43.x stable, compatible torch 2.4.0+cu121.
bitsandbytes==0.43.3

# F5-TTS (PyPI — le tag v1.0.0 sur le repo git n'existe pas)
f5-tts>=1.0.0

# RVC dependencies
# Note : fairseq retiré (stagne depuis 2022, conflit hydra-core>=1.3 avec
# f5-tts). Hubert est chargé via transformers.HubertModel (facebook/
# hubert-base-ls960). Téléchargé automatiquement dans HF_HOME au 1er appel.
faiss-cpu>=1.7.4
praat-parselmouth>=0.4
pyworld>=0.3
torchcrepe>=0.0.20
ffmpeg-python>=0.2

# Audio
soundfile>=0.12
librosa>=0.10
numpy<2.0  # compat torch 2.4
scipy>=1.10

# Utils
pydantic>=2
msgpack>=1.0

# VoiceBridge Worker — Python deps (CUDA 12.1, PyTorch 2.4)

# RunPod SDK
runpod>=1.7

# PyTorch + torchaudio CUDA 12.1 — installés en RUN dédié dans Dockerfile
# (pip install ... --index-url https://download.pytorch.org/whl/cu121).
# Pas de pin ici : le suffixe +cu121 n'est plus géré par pip moderne via
# --extra-index-url (pip cherche sur PyPI et ne trouve pas la wheel CUDA).

# Transformers ecosystem
# /!\ Pin strict transformers==4.46.3 : à partir de 4.50, transformers a
# introduit integrations/moe.py qui appelle torch.library.custom_op avec
# une signature exigeant torch 2.5+. Avec torch 2.4 (pinné pour CUDA 12.1),
# crash "infer_schema(func): unsupported type torch.Tensor" à l'import du
# module M2M100 (architecture de NLLB).
# Refs : HF transformers issues #37238 et #38263.
# 4.46.3 = dernière 4.46 stable connue compatible torch 2.4.
transformers==4.46.3
accelerate>=0.30
huggingface-hub>=0.26
sentencepiece>=0.2     # tokenizers NLLB
sacremoses>=0.1        # tokenizers OPUS-MT

# CTranslate2 pour optimisation Whisper / OPUS-MT
ctranslate2>=4.4
faster-whisper>=1.0

# Quantization — retiré : NLLB est désormais chargé en FP16 safetensors
# (pas INT8) car bitsandbytes exige pytorch_model.bin qui n'est plus sur
# le Volume (supprimé pour économiser 5 Go ; FP16 = 5 Go VRAM, OK sur 4090).

# F5-TTS (PyPI — le tag v1.0.0 sur le repo git n'existe pas)
f5-tts>=1.0.0

# RVC dependencies
# Note : fairseq retiré (stagne depuis 2022, conflit hydra-core>=1.3 avec
# f5-tts). Hubert est chargé via transformers.HubertModel (facebook/
# hubert-base-ls960). Téléchargé automatiquement dans HF_HOME au 1er appel.
faiss-cpu>=1.7.4
praat-parselmouth>=0.4
pyworld>=0.3
torchcrepe>=0.0.20
ffmpeg-python>=0.2

# Audio
soundfile>=0.12
librosa>=0.10
numpy<2.0  # compat torch 2.4
scipy>=1.10

# Utils
pydantic>=2
msgpack>=1.0
